<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
    <item android:title="Operations">
        <menu>
            <item
                android:id="@+id/admin_dashboard"
                android:icon="@drawable/ic_profile"
                android:title="Dashboard" />
            <item
                android:id="@+id/admin_changepass"
                android:icon="@drawable/ic_chat"
                android:title="Change Password" />
            <item
                android:id="@+id/admin_manage_type"
                android:icon="@drawable/ic_message"
                android:title="Manage Laundry Types" />
            <item
                android:id="@+id/admin_manage_orders"
                android:icon="@drawable/ic_message"
                android:title="Manage Orders" />
            <item
                android:id="@+id/admin_confirm_orders"
                android:icon="@drawable/ic_message"
                android:title="Confirmed Orders" />
            <item
                android:id="@+id/admin_logout"
                android:icon="@drawable/ic_message"
                android:title="Logout" />
        </menu>
    </item>
    </group>

    <group android:checkableBehavior="single">
    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share1"
                android:icon="@drawable/ic_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send1"
                android:icon="@drawable/ic_send"
                android:title="Send" />
        </menu>
    </item>
    </group>
</menu>